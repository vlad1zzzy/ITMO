/*
package expression;

import expression.exceptions.ExpressionException;
import expression.generic.TripleExpression;
import expression.generic.operations.Operation;

public class Pow2<T> extends AbstractFunctionUnary<T> {

    public Pow2(TripleExpression<T> first, Operation<T> type) {
        super(first, type);
    }

    protected T calculate(T x) throws ExpressionException {
        return type.not(x);
    }
}*/
